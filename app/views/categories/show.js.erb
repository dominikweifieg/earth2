$("#category_<%=@category.id%>").after('<%= render partial: 'categories/question', collection: @category.questions, locals: {category: @category} %>')
$("#category_<%=@category.id%>").replaceWith('<%= render partial: 'apps/hide_category', locals: {category: @category}%>')